apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "second-chart.fullname" . }}
  labels:
    {{- include "second-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "second-chart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "second-chart.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "second-chart.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
